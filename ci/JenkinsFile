pipeline {
    agent any

    tools {
        maven 'maven 3.9.11' // Match the name in Jenkins tool config
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/codersundaram/JavaApplication.git'
            }
        }
        stage('Build') {
            steps {
                bat 'mvn clean install'
            }
        }
       /*  stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
        stage('Publish Test Results') {
            steps {
                junit 'target/surefire-reports *//*.xml'
            }
        } */
    }
}
